"""Telegram engine — MTProto/Bot-API loop over operator-seeded public channels.
Deterministic (no algorithmic feed), text-only (channel message + discussion
replies): no vision, no engagement, no tired-feed/canary/daytime. Exposes
``run_session``.
"""

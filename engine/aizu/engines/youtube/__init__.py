"""YouTube engine — Data API v3 loop over seeded channels + search queries.
Text-only (title+description relevance, comment-thread matching): no vision,
no engagement, no tired-feed/canary/daytime. Exposes ``run_session``.
"""

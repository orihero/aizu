// AIZU Worker — Tauri build script (Phase 6 SCAFFOLD, UNCOMPILED).
//
// Standard Tauri 2.x build entry point: reads tauri.conf.json and generates the
// context/codegen the app links against. Requires the `tauri-build` build-dependency.
// This has NOT been run — `cargo check` by an engineer with the toolchain will exercise it.

fn main() {
    tauri_build::build();
}

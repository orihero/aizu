# Loaded by Vite only in --mode demo (npm run dev:demo).
# Flips main.tsx over to the seeded, deterministic demo repository
# (src/demo/createDemoRepository.ts) instead of the real HTTP repository —
# no bridge server required. Seed data lives in src/demo/demoFixtures.ts.
VITE_DEMO_CAPTURE=1
